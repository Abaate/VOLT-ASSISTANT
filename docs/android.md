# Android and voice

The Android client should use Kotlin Compose, Android `SpeechRecognizer`, and `TextToSpeech`, then send text to `/ws`. Android actions must use official intents and confirmation UI. This repository includes the server protocol and Android integration contract; building APKs requires an Android SDK/Gradle environment.
