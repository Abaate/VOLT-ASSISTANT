# Troubleshooting

Use `volt doctor`, check `ollama list`, verify the configured workspace, and inspect HTTP status from `/health` and `/status`. If remote Android access fails, confirm VPN routing and API token configuration rather than exposing port 8080 publicly.
