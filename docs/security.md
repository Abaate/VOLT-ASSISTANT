# Security

Never commit `.env`. Configure `API_TOKEN` for any non-loopback deployment. The workspace sandbox prevents file tools from escaping the configured root. CRITICAL tools are never auto-approved. Review commands and use a dedicated unprivileged OS account.
