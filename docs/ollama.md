# Ollama

VOLT calls `/api/chat` with streaming enabled and configurable model, temperature, top-p, top-k, and prediction limits. Use a model tag available in your Ollama installation; the default is `qwen3-coder-next` as requested. Context and concurrency should be kept low on 8 GB RAM systems.
